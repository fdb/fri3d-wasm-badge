[package]
name = "fri3d-esp32"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ESP32 port for Fri3d Badge (future)"

[dependencies]
fri3d-runtime = { workspace = true, default-features = false }
# fri3d-wasm = { workspace = true }  # TODO: Add when implementing

# ESP32 dependencies (uncomment when implementing)
# esp-idf-hal = "0.42"
# embedded-hal = "0.2"
